# Cloud Build config for alerter image.
# Triggered manually or via build-and-push.sh.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/alerter:latest
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/alerter:$SHORT_SHA
      - -f
      - src/alerter/Dockerfile
      - .
images:
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/alerter:latest
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/alerter:$SHORT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 600s
