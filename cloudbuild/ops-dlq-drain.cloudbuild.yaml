# Cloud Build config for ops-dlq-drain image.
# Triggered manually or via build-and-push.sh.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/ops-dlq-drain:latest
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/ops-dlq-drain:$SHORT_SHA
      - -f
      - src/ops/dlq_drain/Dockerfile
      - .
images:
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/ops-dlq-drain:latest
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/ops-dlq-drain:$SHORT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 600s
