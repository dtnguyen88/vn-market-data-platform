# Cloud Build config for curate image.
# Triggered manually or via build-and-push.sh.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/curate:latest
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/curate:$SHORT_SHA
      - -f
      - src/curate/Dockerfile
      - .
images:
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/curate:latest
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/curate:$SHORT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 600s
