# Cloud Build config for publisher image.
# Triggered manually or via build-and-push.sh.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/publisher:latest
      - -t
      - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/publisher:$SHORT_SHA
      - -f
      - src/publisher/Dockerfile
      - .
images:
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/publisher:latest
  - asia-southeast1-docker.pkg.dev/$PROJECT_ID/vn-market/publisher:$SHORT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 600s
